# ~/.tmuxinator/datMulti.yml

name: datNET
root: ~/projects/datNET

windows:
  - multi:
      layout: main-vertical
      panes:
        - cd fs-assemblyInfo-utils
        - cd fs-fake-targets
        - cd fs-filepath-utils
        - cd fs-project-template
        - cd fs-version-utils
